/*  Votre code Javascript vient ici:
      - Vous pourrez visualiser le résultat en ouvrant index.html dans votre navigateur.
      - N'oubliez pas de sauvegarder pour afficher les changements.
*/

// On assigne la valeur "2" à la variable "a".
let a = 2;
// Donc la variable "a" a pour valeur "2".

// On multiplie la valeur de la variable "a" par "2",
// Puis on ajoute "1".
// Ansi on assigne la valeur de tous ces calculs à la variable "x".
let x = 1 + (a *= 2);
// Donc la variable "x" a pour valeur "5".
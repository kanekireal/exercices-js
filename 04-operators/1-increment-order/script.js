/*  Votre code Javascript vient ici:
      - Vous pourrez visualiser le résultat en ouvrant index.html dans votre navigateur.
      - N'oubliez pas de sauvegarder pour afficher les changements.
*/

// On déclare deux variables, tous deux assigné de la valeur "1".
let a = 1, b = 1;

let c = ++a; // ?
// On incrémente la valeur de la variable "a"
// puis on assigne la valeur à la variable "c".
// La variable "c" a donc pour valeur "2".

let d = b++; // ?
// On assigne la valeur de la variable "d".
// Puis on incrémente la valeur de la variable "b".
// La variable "d" a donc pour valeur "1".

// L'incrémentation, c'est ajouté "1" à la valeur de la variable ou l'on applique ce dernier.
// Selon son placement "++variable" ou "variable++", la précédence à pour un impact
// d'assigné la valeur de la variable incrémenté (pour "++variable")
// ou d'assigné la valeur puis d'incrémenté (pour "variable++").